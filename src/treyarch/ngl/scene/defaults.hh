#pragma once

#include "treyarch/ngl/scene/scene.hh"

namespace treyarch { namespace ngl {
    scene* set_default_scene_state();
    scene* clear_and_set_default_scene_state(scene* value);
}} // treyarch::ngl
